{
  "name": "issue",
  "version": "0.0.0",
  "description": "Browser code for fancy issue features",
  "main": "index.mjs",
  "scripts": {
    "build": "esbuild index.mjs --bundle --outfile=bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lit-html": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.7"
  }
}
