(executable
 (public_name issue)
 (name main)
 (libraries 
  ;; app library
  issue
  ;; regex
  str 
  ;; FS operations, supports windows too
  unix
  ;; markdown
  omd))

;; set O2 for a release build
(env
 (release
   (ocamlopt_flags (:standard -O2))))
