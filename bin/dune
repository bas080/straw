(executable
 (public_name issue)
 (name main)
 (libraries 
  ;; app library
  issue
  ;; stdlib
  core
  ;; regex
  str 
  ;; markdown
  omd))

;; set O2 for a release build
(env
 (release
   (ocamlopt_flags (:standard -O2))))
