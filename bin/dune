(executable
 (public_name issue)
 (name main)
 (libraries 
  issue
  core
  ;; CLI argument parsing
  core_unix.command_unix))

;; set O2 for a release build
(env
 (release
   (ocamlopt_flags (:standard -O2))))
