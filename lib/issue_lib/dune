(library
 (name issue_lib)
 (libraries
 ;; path handling
 fpath
 ;; regex
 str
 ;; markdown
 omd))

;; load template.html into binary
(rule
 (with-stdout-to
  template.ml
  (progn
   (echo "let html = {|")
   (cat resources/template.html)
   (echo "|}"))))
